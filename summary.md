# API Types Lab - סיכום

## מבוא קצר:
`API` הפרוטוקול שמאפשר לשתי מערכות נפרדות לתקשר בינהן ולהעביר מידע. יש הרבה סוגי `api` כיון שכל סוג מותאם לצורך אחר. יש פרוטוקולים שדורשים מבנה של בקשה - תשובה קלאסי כמו `REST`, `GraphQL`, `SOAP`, `gRPC`. יש פרוטוקולים שדורשים ערוץ תקשורת פתוח בזמן אמת, כמו `WebSocket`, `SSE`. `Webhooks` הוא פרוטוקול שמתאים לדיווח על אירועים כיון שהתקשורת בו בכיוון ההפוך. `MCP` הוא תקן שנועד להעביר כלים למודל שפה. ו-`CLI` נועד לתת גישה דרך שורת הפקודה למערכת ההפעלה.

## פירוט:
### 01 REST
1. **מה הוא עושה ואיך הוא עובד?** מבוסס על פרוטוקול `HTTP`, יש ארבע בקשות `GET`, `POST`, `PUT`, `DELETE` וכל בקשה יחודית ללינק ספציפי. הערוץ הוא Stateless.
2. **מתי משתמשים?** כשרוצים לחבר שרת לדפדפן, עבור שירותי ווב.
3. **יתרון וחיסרון:** היתרון הוא שהפרוטוקול פשוט יחסית וקל לעשות קאשינג בגלל שלכל בקשה יש סוג וpath יחודי. החסרון הוא שלפעמים מוחזר יותר או פחות מידע ממה שהלקוח היה צריך בגלל שאין גמישות בבקשות.
4. **תובנה שלי מהמימוש:** מאד נוח להשתמש באנדפוינט `/docs` ומאד קל שהוא נור אוטומטית עם `pydentic`.

### 02 GraphQL
1. **מה הוא עושה ואיך הוא עובד?** מבוסס גם כן על `HTTP`, יש אנדפוינט אחת מסוג `POST`, הלקוח שולח שאילתא מפורטת על המידע שהוא רוצה לקבל, השדות והישויות, והמידע הזה בדיוק מוחזר.
2. **מתי משתמשים?** עבור אפליקציות פרונטאנד מורכבות שצריכות נתונים מכמה מקורות.
3. **יתרון וחיסרון:** היתרון הוא שה-Over fetching נפתר בגלל הגמישות שהשאילתא מאפשרת. החסרון הוא שאי אפשר לעשות קאשינג לפי `url` כי הוא זהה לכל הבקשות.
4. **תובנה שלי מהמימוש:** -מעניין לשנות את השאילתא בקליינט ולראות איך ה`json` שחוזר נשתנה בלי לגעת בשרת.

### 03 SOAP
1. **מה הוא עושה ואיך הוא עובד?** פרוטוקול קשיח מבוסס `xml`. מוגדר באמצעות חוזה בקובץ `WSDL` שמגדיר בדיוק את הפעולות, המבנים והטיפוסים.
2. **מתי משתמשים?** מערכות Enterprise ותיקות, בנקים ומערכות ממשלתיות.
3. **יתרון וחיסרון:** היתרון המרכזי הוא שיש חוזה קשיח ותקני אבטחה מובנים. החסרון הוא הסרבול שנובע מהקשיחות של החוזה, התקורה של ה-`xml` וחוסר הנוחות שבשימוש בספריות ישנות.
4. **תובנה שלי מהמימוש:** בהרצה ראיתי את הפער בין פרוטוקולים מודרנים לבין זה. קובץ ה-`WSDL` באמת מונע טעויות מצד הלקוח אבל דורש תשתית כבדה כדי לייצר ולקרוא אותו.

### 04 gRPC
1. **מה הוא עושה ואיך הוא עובד?** מנגנון שמבוסס על `HTTP/2`. ממירים את המידע למבנה בינארי וככה הלקוח יכול להפעיל מרחוק פונקציות של השרת במהירות וביעילות.
2. **מתי משתמשים?** שימושי לתקשורת פנימית בין `microservices` עם הרבה ביצועים בגלל המהירות. לדוגמא: Netflix, Uber.
3. **יתרון וחיסרון:** היתרון המרכזי הוא ביצועים גבוהים בגלל התקורה המינימלית של הפורמט הבינארי ותמיכה בשידור דו כיווני. החסרון הוא שהפורמט לא קריא לבני אדם וקשה לחשוף ישירות לדפדפן.
4. **תובנה שלי מהמימוש:** כשקימלפתי את קבצי ה`.proto` לקבצי הפייתון ראיתי איך שגיאות של טיפוסים ושמות מתגלות בשלב הזה עוד לפני ההרצה. 

### 05 Websocket
1. **מה הוא עושה ואיך הוא עובד?** פרוטוקול מעל חיבור `TCP` אחד רציף שמאפשר העברת הודעות בשני הכיוונים בכל רגע. 
2. **מתי משתמשים?** אפליקציות של צ׳אט בלייב, אפליקציות של עריכה משותפת ולוחות מסחר. לדוגמא התקשורת של slack.
3. **יתרון וחיסרון:** יש Latency נמוך בגלל שהשרת יכול פשוט לדחוף מידע בלי לבקש קודם, זה יתרון מרכזי. חסרון מרכזי הוא בגלל שכל חיבור הוא Stateful קשה לעשות סקיילינג כי אי אפשר פשוט להוסיף עוד שרתים בלי שתהיה תקשורת בינהם.
4. **תובנה שלי מהמימוש:** ראיתי איך החיבור נשאר פתוח ברקע כל הזמן עד שהלקוח מנתק אותו. הרצתי גם קליינט דרך הדפדפן וזה ממש מגניב :)

### 06 Webhooks
1. **מה הוא עושה ואיך הוא עובד?** מבוסס על אירועים. שרת שולח בקשת `HTTP POST` לכתובת Callback שהוגדרה מראש אצל הלקוח ברגעשאירוע מתרחש.
2. **מתי משתמשים?** כאשר מערכת צריכה לקבל הודעות אסינכרוניות ממערכת חיצונית. לדוגמא השרת של `CI/CD` צריך לקבל מגיטהאב עדכון כאשר יש `push` חדש כדי להתחיל לפעול.
3. **יתרון וחיסרון:** בזכות הפרוטוקול הזה לא צריך לעשות Polling וזה חוסך במשאבים. חסרון יכול להיות שהשרת השולח תלוי בזמינות של המקבל, וצריך לנהל מנגונים של Retries.
4. **תובנה שלי מהמימוש:** הבנתי למה צריך את חתימה הדיגיטלית כדי להקשות על זיוף בקשות מגורמים אחרים עוינים.

### 07 MCP
1. **מה הוא עושה ואיך הוא עובד?** תקן המגדיר ממשק אחיד לחיבור ודלי שפה לכלים ומידע חיצוני (Tools, Resources, Prompts)
2. **מתי משתמשים?** כאשר רוצים לחבר סוכני AI למאגר נתונים ולמערכת הפעלה. לדוגמא חיבור Cursor או Claude לדאטה בייס מקומי.
3. **יתרון וחיסרון:** יתרון מרכזי הוא שיש סטנדרט אחיד ולא צריך לכתוב אינטגרציה ספציפית לכל מודל. השימוש של מודל השפה בכלים תלוי איכות התיעוד, אפשר לראות את זה כחסרון.
4. **תובנה שלי מהמימוש:** הבנתי את החשיבות של ניסוח Docstring ברור שהמודל ידע להשתמש בו כמו שצריך. וגם הבנתי איך להשתמש ב-MCP Inspector בשביל לבחון את השרת ולראות את זה ויזואלית.

### 08 CLI
1. **מה הוא עושה ואיך הוא עובד?** ממשק משתמש לקריאות מערכת או רשת בצורה נוחה דרך שורת הפקודה.
2. **מתי משתמשים?** כלי לניהול תשתיות, פיתוח ואוטומציה. לדוגמא `gh`, `aws cli`.
3. **יתרון וחיסרון:** הפרוטוקול מתאים לסקריפטים ואוטומציה ללא צורך בממשק גרפי. אבל הוא לא בדיוק פרוטוקול רשת אלא יותר שכבת הפשטה.
4. **תובנה שלי מהמימוש:** הבנתי שפקודת `CLI` פשוטה כמו `get-post 1` מבצעת מאחורי הקלעים קריאת `HTTP GET` רגילה ומציגה את הפלט בצורה יפה.

### 09 SSE
1. **מה הוא עושה ואיך הוא עובד?** תקשורת מתמשכת בכיוון אחד, מהשרת ללקוח מבוססת על `HTTP` רגיל בפורמט טקסטואלי.
2. **מתי משתמשים?** הזרמת טוקנים ממודל AI, עדכוני חדשות, מניות לדוגמא הזרמה של תשובה מצ׳אט GPT.
3. **יתרון וחיסרון:** יתרון - פשוט לממש מעל `HTTP` סטנדרטי. אבל אי אפשר לשלוח מידע בחזרה מהלקוח לשרת, זה יכול להיות חסרון.
4. **תובנה שלי מהמימוש:** ראיתי איך השורות מגיעות בזו אחר זו מהשרת ללקוח בלי שצריך לפתוח בקשה לכל אחת.

### 10 tRPC
1. **מה הוא עושה ואיך הוא עובד?** המטרה של הפרוטוקול היא להשיג בטיחות של טיפוסי נתונים מקצה לקצה בין השרת ללקוח, על ידי שיתוף הטיפוסים של השרת עם הלקוח, או על ידי הפקה של קוד לקוח אוטומטית לפי סכמה של OpenAPI.
2. **מתי משתמשים?** במערכות Full Stuck שבהם השרת והלקוח בנויים בשפה אחידה. לדוגמא מערכות `Next.js` מודרניות עם `tRPC`.
3. **יתרון וחיסרון:** שגיאות בחוסר תיאום של טיפוסים בין השרת ללקוח מתגלות לפני ההרצה בזמן פיתוח וקומפילציה. מצד שני השרת והלקוח לויים מאד אחד בשני.
4. **תובנה שלי מהמימוש:** הבנתי איך הכלי `openapi-python-client` מפיק ספריית לקוח מלאה מקובץ `openapi.json` וכך בכל שינוי בשרת השינוי מתעדכן אוטומטית בלקוח.


## טבלת השוואה מסכמת:
| סוג API | Transport | פורמט נתונים | כיווניות | מתי להשתמש | 
|---|---|---|---|---|
| REST | `HTTP/1.1` | `JSON` | בקשה-תשובה | שירותי ווב ציבוריים, מערכות שדורשות קאשינג רחב. |
| GraphQL | `HTTP` | `JSON` | בקשה-תשובה | ממשקי פרונטאנד שדורשים שליפה מדוייקת של שדות ממספר ישויות בבת אחת.|
| SOAP | `HTTP / SMTP` | `XML` | בקשה-תשובה | מערכות Enterprise, בנקים וגופים פיננסיים המחייבים חוזה נוקשה ותקני `WS-Security`. |
| gRPC |	`HTTP/2` | `Protobuf` (בינארי) | כולל streaming| תקשורת עם הרבה ביצועים ותקרה נמוכה בין רכיבים פנימיים. |
| WebSocket |	`TCP`	| טקסט / בינארי	| דו-כיווני| אפליקציות זמן אמת דו כיווניות שבהם שני הצדדים יוזמים שליחת מידע.|
| Webhooks| `HTTP` | `JSON` |	server -> client| כשצריך לקבל הודעות אסינכרוניות על אירוע שהתרחש במערכת חיצונית בלי לעשות polling. |
| MCP	| `stdio/HTTP` | `JSON-RPC` | בקשה-תשובה | כשצריך לחשוף מידע וכלים ופרומפטים מובנים למודלי שפה וסוכני AI. |
| SSE | `HTTP` | טקסט	| server -> client | בשביל הזרמת עדכונים מהשרת ללקוח כמו טוקנים, התראות ופידים. |


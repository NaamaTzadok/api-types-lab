## Lab Notes:

#### 01-Rest
> **Question:** מדוע `REST` מתואר כ-`stateless`, ומה היתרון של זה ביכולת לבצע `scaling` לשרת על פני מספר מכונות?

> **Answer:** הארכיטקטורה של `REST` בנויה בצורה כזו שהשרת לא שומר מידע בזכרון על המשתמש. כל בקשה מהלקוח היא נפרדת ובלתי תלויה באחרות. יש לזה יתרונות, אפשר לפצל את הבקשות על פני כמה שרתים נפרדים בלי לדאוג לתלות בינהם. ואם שרת אחד קורס אפשר להעביר את כל הבקשות לשרת אחר בלי לדאוג למידע מסויים. יש לזה גם חסרון שכל בקשה מעט יותר כבדה כיון שהיא מכילה את כל הנתונים והקונטקסט שהשרת צריך בשביל לטפל בה.


#### 02-GraphQL
> **Question:** איזו בעיה ב-`REST` פותר `GraphQL`, ומדוע דווקא בגלל הפתרון הזה קאשינג הופך למורכב יותר?

> **Answer:** ב`REST` יש לכל אנדפוינט נתיב מוגדר ותבנית קבועה של נתונים שמוחזרים באותה אנדפוינט. ב`GraphQL` לעומת זאת, יש אנדפוינט אחת שבה הקליינט מגדיר איזה נתונים הוא צריך ומקבל רק אותם. כך הלקוח לא מקבל מידע מיותר, ומקבל בדיוק מה שהוא צריך. החסרון הוא שב-`REST` יכלנו לבצע קאשינג על סמך ה- `URL` המדויק, ופה זה נעשה יותר מורכב כי הכל נמצא באנדפוינט אחת. ולכן צריך להשתמש בספריות צד לקוח ששומרות את הנתונים לפי ה-`id`, או על ידי שאילתות שמיוצאות מראש בזמן ה`build`. 

#### 03-SOAP
> **Question:** מה מספק קובץ ה-`WSDL` שאין לו מקבילה מובנית ב-`REST`, ומדוע זה רלוונטי במיוחד למערכות בנקאות ו-`enterprise`?

> **Answer:** הקובץ מספק מבנה של מערכת טיפוסים קשיחה ואוטומטית וזה מונע שגיאות ומאפשר לזהות שגיאות עוד לפני שהן מגיעות לשרת. ובנוסף לזה יש תקן מובנה בקובץ שמאפשר להצפין בו חלקים ספציפים, מה שאומר שההצפנה היא ברמת ההודעה ולא ברמת הערוץ שבו ההודעה נשלחת כמו ב- `https` ב-`rest`. כלומר הפרוטוקול הזה מאד בטוח ולכן מתאים למערכות ממשלתיות ופיננסיות.

#### 04-gRPC
> **Question:** מדוע `gRPC` מתאים יותר לתקשורת פנימית בין `microservices` מאשר לחשיפה ישירה ל-`browser`, ומה היתרון של פורמט בינארי על `JSON` מבחינת ביצועים?

> **Answer:** `gRPC` מחייב עבודה מעל `HTTP/2` זאת אומרת שהרבה בקשות יכולות להשלח בחיבור אחד, מה שמקטין את `הlatency`, ולכן הוא יעיל בתקשורת בין `microservices` אבל בתקשורת עם דפדפן גם אם אפשר להשתמש ב-`HTTP/2` אי אפשר ממש לנהל את הסטרימינג של הבקשות אלא אם כן משתמשים בשרת תיווך שמתרגם את הבקשה.
בנוסף הפרוקטוקול משתמש בפורמט בינארי שהיתרון שלו על פני פורמט `json` שהוא תופס פחות רוחב פס ופחות עיבוד של המעבד. ודפדפנים משתמשים בפורמטים טקסטואלים.
סיבה נוספת היא שבכל שינוי של הדפדפן נצטרך לקמפל את קובץ ה-`.poto` בשביל להתאים את הקוד לדפדפן. פרוטוקולים אחרים כמו `REST` או `GraphSQL` יותר גמישים.

#### 05-WebSocket
> **Question:** מה ההבדל המהותי בין `WebSocket` ל-`REST` מבחינת מי יוזם את התקשורת, ומדוע `REST` אינו מתאים ליישום צ'אט בזמן אמת?

> **Answer:** בפרוטוקול `WebSocket` ה-`Cient` מבקש ליצור את החיבור בהתחלה ואם השרת מאשר נוצר חיבור שבו גם הלקוח וגם השרת יכלים לשלוח הודעות מתי שהם רוצים, כל עוד החיבור נשאר פתוח. אין חשש להתנגשות בין ההודעות כי יש ערוצים נפרדים לשליחה וקבלה. וזה בשונה מ-`REST` שבו השרת שולח הודעות רק אם הלקוח ביקש ממנו. במערכות צ׳אטים בזמן אמת יש מידע שמתעדכן בשרת והוא צריך להשלח ללקוח בזמן אמת בלי שהלקוח יצטרך לבקש את המידע ולכן נעדיף להשתמש בפרוטוקול `WebSocket` על פני `REST`.

#### 06-Webhooks
> **Question:** מהו ההבדל בין `polling` ל-`webhook` מבחינת צריכת משאבים, ומדוע אימות חתימה הוא חובה ולא אופציה?

> **Answer:** בשיטת ה-`polling` הלקוח מבקש כל הזמן את המידע מהשרת המרוחק, ואילו ב-`webhook` השרת המרוחק שולח בקשת `post` לשרת של הלקוח. זה חוסך ברוחב פס וזמן `CPU` כי במקום לשלוח כל הזמן הרבה בקשות נשלחת בקשה אחת לכל אירוע. מכיון שהאנדפוינט חשופה לבקשות משרת מרוחק זה חושף אותה להודעות מזויפות, ולכן מטעמי אבטחה לכל בקשת `post` כזו יש חתימה דיגיטלית מסויימת והלקוח (השרת שלי) מקבל רק הודעות שמוצפנות על ידי אותה חתימה דיגיטלית שנמצאת אצלו.

#### 07-MCP
> **Question:** מי הצרכן המיועד של `MCP server`, ומדוע ניסוח ה-`docstring` של `tool` הוא קריטי לתפקוד הנכון, בניגוד לתיעוד רגיל ב-`REST`?

> **Answer:** הצרכן הוא מודל שפה כלשהו, ניסוח ה-`docstring` עוזר למודל להבין את השימוש של הפונקציה, מתי להשתמש בכלי ואיך. ולכן חשוב לנסח אותו בצורה מדויקת כדי שהמודל ידע לעשות בו שימוש נכון. וזה בניגוד ל-`REST` שהלקוח שלו הוא אפליקצית דפדפן קבועה שבזמן שהיא נכתבת נקבע השימוש המדויק ב-`API` על ידי המתכנת.

#### 08-CLI
> **Question:** מדוע `CLI` אינו נחשב סוג `API` אלא ממשק, ומה קורה ברשת מאחורי הקלעים כאשר מריצים פקודה כמו `gh repo list`?

> **Answer:** `API` הוא הממשק של אפליקציה שהלקוח מדבר איתה דרך האינטרנט, ו-`CLI` הוא הממשק ידידותי למשתמש דרך שורת הפקודה לא בהכרח עבור תקשורת ברשת.
לדוגמא כאשר מריצים את הפקודה `gh repo list` ה-`CLI` של גיטהאב שמותקן לוקאלית מפענח את הפקודה ומתרגם אותה לבקשת `http` מתאימה שנשלחת לשרתים של גיטהאב (`api` רגיל) ומחזיר את התוצאה. 

#### 09-SSE
> **Question:** מתי כדאי לבחור `SSE` על פני `WebSocket`, ולמה `SSE` מתאים במיוחד לסטרימינג של תשובות מ-`LLM`?

> **Answer:** `SSE` מתאים כאשר רוצים להזרים מידע בכיוון אחד - מהשרת ללקוח. ואילו `WebSocket` משמש להעברת מידע בשני הכיוונים, הוא מתאים בשביל מערכות של צ׳אטים בזמן אמת. ולכן כאשר רוצים להזרים תשובות מ-`LLM` מהשרת ללקוח משתמשים ב-`SSE` כדי שהמידע יוזרם בזמן אמת והמשתמש לא יראה דף ריק בזמן שהשרת מכין את כל התשובה ושולח אותה בבת אחת.

#### 10-tRPC
> **Question:** מהי המשמעות של `end-to-end type safety`, ואיזו בעיה נפוצה בפיתוח client-server הוא מונע?

> **Answer:** E`nd-to-end type safety` זו תכונה שמאפשרת להגדיר בצורה אחידה לשרת וללקוח את סוגי המבנים והמשתנים. זה מונע באגים בזמן ריצה כאשר הלקוח והשרת לא מתואמים בשמות משתנים, לפעמים כתוצאה משינוי שנעשה בשרת. ולכן `tRPC` פותר את הבעיה, כל פונקציה שנכתבת בשרת משתקפת ללקוח כבר בזמן כתיבת הקוד והקומפילציה, כך הלקוח יכול לקרוא לפונקציה כאילו היא מקומית אצלו בלי להתבלבל בטיפוסי נתונים.

#### ty & ruff Checks
> **Question:** מדוע הרצת `lint` ובדיקת טיפוסים אוטומטיים ב-`CI` חשובה בצוות פיתוח, ומה היתרון של כלים כמו `ruff` ו-`ty` (כתובים ב-`Rust`) על פני הדור הקודם (`flake8, black, mypy`)?

> **Answer:** הרצת הבדיקות עוזרת להמנע משגיאות תחביריות בזמן הריצה. מגלים את השגיאות לפני שהקוד מגיע לשימוש. זה חוסך זמן ב-`code review` ושומר על אחידות בקוד ונראות. ההבדל בין הדור החדש שכתוב ב-`Rust` הוא ששפה זו מקומפלת ומהירה יותר בביצועים, ובנוסף הכלי `ruff` מחליף הרבה כלים שונים, וכך יותר קל לנהל את הבדיקות בקובץ קונפיגורציה אחד.